Есть глобальный массив $_POST
Вытаскивать из него так - $_POST['email'];

Есть глобальный массив $_COOKIE
Вытаскивать из него так - $_COOKIE['user'];

trim() - удаляет все пробелы из строки или только лишнее хд

strlen() - возвращает длина строки

использовать elseif
<?php
    if():
?>
<?php
    elseif():
?>

<?php
    endif;
?>

header('Location: /about.php') - переадрисация

setcookie('user' - название куки, 'true, time() + 3600, '/'); - установить куки

Dreeedy
C30Td05UQ209IPhg

Есть user
у каждого user есть множество актов (как минимум о рождении)
у каждого акта есть один user
это дерьмо по сути нужно связать через книгу актов
в ней будет
ид книги, ид юзера, ид акта.

+ таблица населенных пунктов